# Cursor Adapter

Install the portable skill and MCP entry through `hound integrations install cursor`. Cursor does not consume the Claude plugin manifest; adapt `plugins/shared/commands/engine.md` to project rules when a command surface is desired.
