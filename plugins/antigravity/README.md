# Antigravity Adapter

Install the skill and current-release MCP configuration through `hound integrations install antigravity`. Adapt the shared command specification only if the installed release supports prompt commands.
