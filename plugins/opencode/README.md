# OpenCode Adapter

Install the portable skill and MCP configuration through `hound integrations install opencode`. Use the shared engine command specification as an OpenCode prompt command if commands are enabled by the target OpenCode release.
