# Native Harness Fallback

If Hound MCP is unavailable, use bounded native read/search tools to locate the primary error, failing test, and source location. Use the harness's authorized test runner only when reproduction or verification is needed. Label the result `native/advisory`; do not claim Hound schema validation, fingerprinting, history statistics, or gate success. No CLI/TUI installation is required.
